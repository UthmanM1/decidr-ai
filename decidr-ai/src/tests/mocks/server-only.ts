// Stub for the `server-only` package so it can be imported in the Vitest
// (plain Node) environment without throwing. Next.js itself enforces the
// real server-only restriction at build time via webpack.
export {};
